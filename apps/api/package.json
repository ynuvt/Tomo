{
  "name": "api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "db:setup": "wrangler d1 migrations apply tomo-db --local",
    "lint": "echo \"No API linter configured yet\""
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}